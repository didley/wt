# wt GUI (planned)

The GUI lives here in a later milestone: a Wails app reusing
`internal/core`, distributed as a Homebrew cask (`wt`) on macOS and via
Flathub on Linux. See the repository README for the roadmap.
